name: "Backend workflow"
on: 
  push:
    paths: backend/**

jobs:
  greet_from_main:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: run code
        run: echo "Hello world from Backend"

      - name: show file contents
        run: cat backend/hello.txt
